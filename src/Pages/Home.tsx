import React from "react";
import MovieList from "../components/MovieList";

const Home = () => {
  return (
    <div>
      <section>
        <MovieList />
      </section>
    </div>
  );
};

export default Home;
